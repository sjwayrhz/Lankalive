# Database Configuration
# If DEV=true, this URL will be used (local development)
# If DEV=false, DATABASE_URL will be auto-configured to use Docker postgres container
# Database Configuration
# For local development with your own PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Development Mode
# Set to 'true' for local development (uses local DATABASE_URL)
# Set to 'false' for VPS deployment (uses Docker postgres container)
DEV=true

# Domain Configuration
# Only needed for VPS deployment - your domain without http:// or port
# Example: mysite.com (not http://mysite.com or http://mysite.com:49155)
DOMAIN=

# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-here
JWT_SECRET=your-jwt-secret-here

# PostgreSQL Configuration (only used when DEV=false)
# These are used by Docker Compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-postgres-password
POSTGRES_DB=lankalive

# Development Mode
# Set to 'true' for local development, 'false' for Docker/VPS deployment
DEV=true

# Domain Configuration (for VPS deployment)
# Leave empty for local development, set domain for VPS deployment (e.g., yourdomain.com)
DOMAIN=

# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-change-in-production
JWT_SECRET=your-jwt-secret-here-change-in-production

# PostgreSQL Configuration (for Docker)
POSTGRES_PASSWORD=postgres

# Frontend API Configuration
# For local dev: http://127.0.0.1:8000
# For VPS with Nginx reverse proxy: https://yourdomain.com
VITE_API_BASE=http://127.0.0.1:8000


# ===============================================
# 🔧 本地开发配置 (不使用 Docker)
# ===============================================

# Development Mode
# 设为 'true' 以使用本地 DATABASE_URL 和 VITE_API_BASE
DEV=true

# Database Configuration
# 请确保您的本地 PostgreSQL 实例已启动，并替换 'user', 'password', 'dbname'
DATABASE_URL=postgresql://postgres:vwv56ty7@localhost:5432/lankalive_db
# DATABASE_URL=postgresql://postgres:WazHiT7k@localhost:5432/lankalive_db

# Flask Configuration
FLASK_ENV=development
# 务必在生产环境中更改这些密钥！
SECRET_KEY=5c8e2a0f7b91d3c6e4f0a2b9d8c7a6e5b4f3a2d1c0b9a8f7e6d5c4b3a2f1e0d9
JWT_SECRET=a3f0b2e7d6c5b4a9f8e1d0c7b6a5f4e3d2c1b0a9f8e7d6c5b4a3f2e1d0c9b8a7

# Frontend API Configuration
# 指向本地运行的 Flask 后端地址和端口 (Flask 默认可能是 5000 或 8000)
VITE_API_BASE=http://127.0.0.1:8000

# Domain Configuration
# 本地开发不需要域名
DOMAIN=